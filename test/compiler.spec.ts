import { expect } from 'chai';

describe('Example', () => {
  it('shall not pass', async () => {
    expect(true).to.equal(true);
  });

  it('should pass this', async () => {
    expect(true).to.equal(true);
  });
});
